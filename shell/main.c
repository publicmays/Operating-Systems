#include "includes.h"

/*int yyparse();
int readInputForLexer(char *buffer, int *numBytesRead, int maxBytesToRead);
*/

int main() {
	shell_init();
	/*while(1) {
		printPrompt();
		switch(cmd = getCommand()){
			case BYE:
				exit();
				break;
			case ERRORS:
				recover_from_errors();
				break;
			case OK:
				processCommand();
				break;
		}
	}*/
}